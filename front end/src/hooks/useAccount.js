import {useCallback,useEffect,useState} from 'react';import {getAccount} from '../api/account';
export default function useAccount(enabled=true){const [account,setAccount]=useState(null),[loading,setLoading]=useState(enabled),[error,setError]=useState('');const refresh=useCallback(async()=>{if(!enabled)return;setLoading(true);setError('');try{const data=await getAccount();setAccount(data.account||data)}catch(e){setError(e.response?.data?.message||'Unable to load account')}finally{setLoading(false)}},[enabled]);useEffect(()=>{refresh()},[refresh]);return {account,loading,error,refresh}}
