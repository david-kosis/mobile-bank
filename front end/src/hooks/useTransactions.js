import {useCallback,useEffect,useState} from 'react';import {getTransactions} from '../api/account';
export default function useTransactions(enabled=true){const [transactions,setTransactions]=useState([]),[loading,setLoading]=useState(enabled),[error,setError]=useState('');const refresh=useCallback(async()=>{if(!enabled)return;setLoading(true);try{const data=await getTransactions();setTransactions(data.transactions||data||[]);setError('')}catch(e){setError(e.response?.data?.message||'Unable to load transactions')}finally{setLoading(false)}},[enabled]);useEffect(()=>{refresh()},[refresh]);return {transactions,loading,error,refresh}}
