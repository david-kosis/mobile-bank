import {ArrowRight,ShieldCheck} from 'lucide-react';import {money} from '../../utils/currency';export default function TransferReview({data,fee=10,onBack,onConfirm,busy}){const total=Number(data.amount||0)+fee;return <div className="review-card"><div className="review-top"><span>TRANSFER REVIEW</span><ShieldCheck/></div><h2>{money(data.amount)}</h2><p>Review everything before you send.</p><dl><div><dt>Recipient</dt><dd>{data.recipientName}</dd></div><div><dt>Account</dt><dd>···· {String(data.recipientAccount).slice(-4)}</dd></div><div><dt>Amount</dt><dd>{money(data.amount)}</dd></div><div><dt>Fee</dt><dd>{money(fee)}</dd></div><div className="total"><dt>Total</dt><dd>{money(total)}</dd></div></dl><div className="confirm-actions"><button className="secondary" onClick={onBack}>Edit</button><button className="primary" disabled={busy} onClick={()=>onConfirm({...data,fee,total})}>{busy?'Checking status…':'Confirm transfer'} <ArrowRight/></button></div></div>}
