import {ShieldCheck,Building2} from 'lucide-react';export default function RecipientConfirmation({recipient,onConfirm,onBack}){return <div className="confirmation-card"><div className="confirm-icon"><ShieldCheck/></div><small>RECIPIENT FOUND</small><h2>{recipient.name}</h2><p><Building2/> {recipient.bankName||'Bank account'} ···· {String(recipient.accountNumber).slice(-4)}</p><strong>Is this the person you want to send money to?</strong><div className="confirm-actions"><button className="secondary" onClick={onBack}>Change</button><button className="primary" onClick={onConfirm}>Yes, continue</button></div></div>}
