import {useState} from 'react';import {Send,AlertCircle} from 'lucide-react';export default function TransactionIssue({transaction,onClose}){const [issue,setIssue]=useState('');return <div className="issue-card"><div><AlertCircle/><div><b>Report a problem</b><span>{transaction?`Transaction: ₦${Number(transaction.amount||0).toLocaleString('en-NG')} → ${transaction.counterpartyName||'recipient'}`:'Tell us what went wrong.'}</span></div></div><select value={issue} onChange={e=>setIssue(e.target.value)}><option value="">Choose an issue</option><option>I don't recognize this</option><option>Money was deducted but recipient didn't receive it</option><option>Wrong amount</option><option>Transfer is stuck</option><option>Other</option></select><button className="primary" disabled={!issue} onClick={onClose}><Send/>Submit problem</button></div>}
