import {ShieldAlert,CheckCircle2,Clock3} from 'lucide-react';export default function NotificationItem({item}){const Icon=item.type==='security'?ShieldAlert:item.type==='pending'?Clock3:CheckCircle2;return <article className={`notification-item ${item.type||'transaction'}`}><Icon/><div><b>{item.title}</b><span>{item.text}</span><small>{item.time||'Just now'}</small></div></article>}
