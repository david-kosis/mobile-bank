import {Snowflake,Eye,KeyRound,SlidersHorizontal} from 'lucide-react';export default function CardActions({frozen,onFreeze}){return <div className="card-actions">{[[frozen?'Unfreeze card':'Freeze card',Snowflake,onFreeze],[ 'Show details',Eye,()=>{}],['Change PIN',KeyRound,()=>{}],['Spending limit',SlidersHorizontal,()=>{}]].map(([label,Icon,fn])=><button key={label} onClick={fn}><Icon/><span>{label}</span></button>)}</div>}
