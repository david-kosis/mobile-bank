import {BarChart3,TrendingDown} from 'lucide-react';import {money} from '../../utils/currency';export default function SpendingOverview({transactions=[]}){const total=transactions.filter(t=>t.direction==='debit'&&['successful','success'].includes(String(t.status).toLowerCase())).reduce((s,t)=>s+Number(t.amount||0),0);return <section className="insight-card"><div className="section-heading"><div><small>SPENDING</small><h2>This month</h2></div><BarChart3/></div><strong className="insight-total">{money(total)}</strong><div className="mini-chart"><span style={{height:'35%'}}/><span style={{height:'55%'}}/><span style={{height:'42%'}}/><span style={{height:'70%'}}/><span style={{height:'50%'}}/><span style={{height:'82%'}}/><span style={{height:'62%'}}/></div><p><TrendingDown/> Track your spending from your real transaction history.</p></section>}
