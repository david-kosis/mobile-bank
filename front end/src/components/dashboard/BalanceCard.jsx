import {Eye,EyeOff,Copy,Check} from 'lucide-react';import {useState} from 'react';import {money} from '../../utils/currency';
export default function BalanceCard({account}){const [visible,setVisible]=useState(true),[copied,setCopied]=useState(false);const copy=async()=>{if(!account?.accountNumber)return;await navigator.clipboard?.writeText(account.accountNumber);setCopied(true);setTimeout(()=>setCopied(false),1600)};return <section className="balance-card"><div className="balance-top"><div><span>Available balance</span><button className="icon-button" onClick={()=>setVisible(!visible)} aria-label="Toggle balance">{visible?<Eye/>:<EyeOff/>}</button></div><span className="balance-chip">NGN · Active</span></div><strong>{visible?money(account?.availableBalance):'₦••••••••'}</strong><div className="balance-bottom"><span>Account ···· {String(account?.accountNumber||'').slice(-4)}</span><button onClick={copy}>{copied?<Check/>:<Copy/>}{copied?'Copied':'Copy'}</button></div></section>}
