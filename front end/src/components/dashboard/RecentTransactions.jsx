import {ChevronRight} from 'lucide-react';import {useNavigate} from 'react-router-dom';import TransactionCard from '../transactions/TransactionCard';import EmptyState from '../common/EmptyState';export default function RecentTransactions({transactions=[]}){const nav=useNavigate();return <section className="recent-section"><div className="section-heading"><div><small>ACTIVITY</small><h2>Recent transactions</h2></div><button onClick={()=>nav('/transactions')}>See all <ChevronRight/></button></div>{transactions.length?<div className="transaction-list">{transactions.slice(0,5).map(t=><TransactionCard key={t._id||t.reference} transaction={t} onClick={x=>nav(`/transactions/${x._id||x.reference}`)}/>)}</div>:<EmptyState title="No activity yet" text="Your transactions will appear here."/>}</section>}
