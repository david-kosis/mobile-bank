import {Send,Download,Upload,ArrowUpFromLine,ReceiptText,Smartphone} from 'lucide-react';import {useNavigate} from 'react-router-dom';const actions=[['Send',Send,'/transfer'],['Receive',Download,'/'],['Deposit',Upload,'/'],['Withdraw',ArrowUpFromLine,'/'],['Bills',ReceiptText,'/'],['Airtime',Smartphone,'/']];export default function QuickActions(){const nav=useNavigate();return <section className="quick-actions">{actions.map(([label,Icon,to])=><button key={label} onClick={()=>nav(to)}><span><Icon/></span><b>{label}</b></button>)}</section>}
