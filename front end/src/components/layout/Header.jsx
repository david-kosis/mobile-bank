import {Menu,Bell,Search,Sun,Moon} from 'lucide-react';import {useLocation,useNavigate} from 'react-router-dom';import {useTheme} from '../../context/ThemeContext';import {useAuth} from '../../context/AuthContext';
export default function Header({onMenu}){const nav=useNavigate(),loc=useLocation(),{theme,toggleTheme}=useTheme(),{user}=useAuth();return <header className="topbar"><button className="mobile-menu" onClick={onMenu}><Menu/></button><div className="top-title"><span>{loc.pathname==='/'?'Overview':'MobileBank'}</span><b>{loc.pathname==='/'?`Good ${new Date().getHours()<12?'morning':new Date().getHours()<18?'afternoon':'evening'}, ${(user?.name||'Customer').split(' ')[0]}`:loc.pathname.slice(1).replace(/-/g,' ')}</b></div><div className="top-actions"><button className="search-trigger"><Search/><span>Search</span></button><button onClick={()=>nav('/notifications')}><Bell/></button><button onClick={toggleTheme}>{theme==='dark'?<Sun/>:<Moon/>}</button><button className="top-avatar" onClick={()=>nav('/settings')}>{(user?.name||'U')[0]}</button></div></header>}
