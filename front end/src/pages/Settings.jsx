import {Moon,Sun,LogOut,ShieldCheck,UserRound,Bell} from 'lucide-react';import {useAuth} from '../context/AuthContext';import {useTheme} from '../context/ThemeContext';import {useNavigate} from 'react-router-dom';export default function Settings(){const {user,logout}=useAuth(),{theme,toggleTheme}=useTheme(),nav=useNavigate();return <div><div className="page-heading"><small>ACCOUNT</small><h1>Settings</h1><p>Manage your account preferences and security.</p></div><div className="settings-profile"><div className="profile-avatar">{(user?.name||'U')[0]}</div><div><b>{user?.name||'Customer'}</b><span>{user?.email}</span></div><button onClick={()=>nav('/profile')}>View profile</button></div><section className="settings-list panel">{[[UserRound,'Personal information','Update your profile'],[ShieldCheck,'Security','Password and account protection'],[Bell,'Notifications','Control important updates']].map(([I,t,d])=><button key={t}><I/><span><b>{t}</b><small>{d}</small></span><b>›</b></button>)}<button onClick={toggleTheme}>{theme==='dark'?<Sun/>:<Moon/>}<span><b>Appearance</b><small>{theme==='dark'?'Dark mode':'Light mode'}</small></span><b>›</b></button></section><button className="danger-button" onClick={logout}><LogOut/>Sign out</button></div>}
