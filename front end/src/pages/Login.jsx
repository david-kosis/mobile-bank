import {useState} from 'react';import {Link,useNavigate} from 'react-router-dom';import {Eye,EyeOff,ShieldCheck} from 'lucide-react';import {useAuth} from '../context/AuthContext';
export default function Login(){const {login}=useAuth(),nav=useNavigate(),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[show,setShow]=useState(false),[busy,setBusy]=useState(false),[error,setError]=useState('');const submit=async e=>{e.preventDefault();setBusy(true);setError('');try{await login({email,password});nav('/')}catch(e){setError(e.response?.data?.message||'Unable to sign in. Check your details.')}finally{setBusy(false)}};return <div className="auth-shell"><div className="auth-showcase"><div className="auth-brand"><span>MB</span><b>MobileBank</b></div><div><small>PERSONAL FINANCE, WITHOUT THE FRICTION.</small><h1>Your money,<br/><em>clearly.</em></h1><p>See what you have, where it went and whether every transfer is truly complete.</p></div><div className="trust-line"><ShieldCheck/> Built around clarity, security and control.</div></div><div className="auth-panel"><div className="mobile-auth-brand"><span>MB</span><b>MobileBank</b></div><div className="auth-copy"><small>WELCOME BACK</small><h2>Sign in to your account</h2><p>Secure access to your everyday finances.</p></div><form onSubmit={submit} className="auth-form"><label>Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com" autoComplete="email" required/></label><label>Password<span className="password-field"><input type={show?'text':'password'} value={password} onChange={e=>setPassword(e.target.value)} placeholder="Enter your password" autoComplete="current-password" required/><button type="button" onClick={()=>setShow(!show)}>{show?<EyeOff/>:<Eye/>}</button></span></label>{error&&<div className="form-error">{error}</div>}<div className="auth-row"><Link to="/forgot-password">Forgot password?</Link></div><button className="primary auth-submit" disabled={busy}>{busy?'Signing you in…':'Sign in'}</button></form><p className="auth-switch">New to MobileBank? <Link to="/register">Create an account</Link></p></div></div>}
