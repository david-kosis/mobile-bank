import {useState} from 'react';import {Link,useNavigate} from 'react-router-dom';import {Eye,EyeOff,ShieldCheck,Check} from 'lucide-react';import {useAuth} from '../context/AuthContext';
export default function Register(){const {register}=useAuth(),nav=useNavigate(),[form,setForm]=useState({name:'',email:'',phone:'',password:'',confirm:''}),[show,setShow]=useState(false),[busy,setBusy]=useState(false),[error,setError]=useState('');const strength=form.password.length>=10?'Strong':form.password.length>=6?'Fair':'Weak';const submit=async e=>{e.preventDefault();if(form.password!==form.confirm)return setError('Passwords do not match.');setBusy(true);setError('');try{await register({name:form.name,email:form.email,phone:form.phone,password:form.password});nav('/')}catch(e){setError(e.response?.data?.message||'Unable to create your account.')}finally{setBusy(false)}};const set=k=>e=>setForm({...form,[k]:e.target.value});return <div className="auth-shell"><div className="auth-showcase"><div className="auth-brand"><span>MB</span><b>MobileBank</b></div><div><small>A CLEARER WAY TO BANK.</small><h1>Start with<br/><em>confidence.</em></h1><p>Create your account and keep every important money decision visible.</p></div><div className="trust-line"><ShieldCheck/> Your financial journey starts with secure access.</div></div><div className="auth-panel"><div className="mobile-auth-brand"><span>MB</span><b>MobileBank</b></div><div className="auth-copy"><small>GET STARTED</small><h2>Create your account</h2><p>Set up your secure MobileBank profile.</p></div><form onSubmit={submit} className="auth-form"><label>Full name<input value={form.name} onChange={set('name')} placeholder="David Kosisochukwu" required/></label><label>Email<input type="email" value={form.email} onChange={set('email')} placeholder="you@example.com" required/></label><label>Phone number<input value={form.phone} onChange={set('phone')} placeholder="08012345678" inputMode="tel"/></label><label>Password<span className="password-field"><input type={show?'text':'password'} value={form.password} onChange={set('password')} placeholder="Create a strong password" minLength="6" required/><button type="button" onClick={()=>setShow(!show)}>{show?<EyeOff/>:<Eye/>}</button></span></label><div className={`password-strength ${strength.toLowerCase()}`}><span/><span/><span/><b>{strength}</b></div><label>Confirm password<input type="password" value={form.confirm} onChange={set('confirm')} placeholder="Repeat your password" required/></label>{error&&<div className="form-error">{error}</div>}<button className="primary auth-submit" disabled={busy}>{busy?'Creating account…':'Create account'}</button></form><p className="auth-switch">Already have an account? <Link to="/login">Sign in</Link></p></div></div>}
