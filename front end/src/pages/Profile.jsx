import {useAuth} from '../context/AuthContext';import {UserRound,ShieldCheck,Bell,Lock,HelpCircle} from 'lucide-react';export default function Profile(){const {user}=useAuth();return <div><div className="page-heading"><small>PROFILE</small><h1>Your profile</h1><p>Your identity and account information.</p></div><div className="profile-hero"><div className="profile-avatar">{(user?.name||'U')[0]}</div><div><h2>{user?.name||'Customer'}</h2><span>{user?.email}</span><em><ShieldCheck/> KYC {user?.kycStatus==='verified'?'Verified':'Status: '+(user?.kycStatus||'not started')}</em></div></div><div className="profile-menu">{[[UserRound,'Personal information'],[ShieldCheck,'Security'],[Bell,'Notifications'],[Lock,'Privacy'],[HelpCircle,'Help & support']].map(([I,l])=><button key={l}><I/><span>{l}</span><b>›</b></button>)}</div></div>}
