import useTransactions from '../hooks/useTransactions';import {money} from '../utils/currency';import Loader from '../components/common/Loader';import ErrorState from '../components/common/ErrorState';export default function Analytics(){const {transactions,loading,error,refresh}=useTransactions(true);if(loading)return <Loader label="Building your insights…"/>;if(error)return <ErrorState text={error} onRetry={refresh}/>;const successful=transactions.filter(t=>['successful','success'].includes(String(t.status).toLowerCase()));const out=successful.filter(t=>t.direction==='debit').reduce((s,t)=>s+Number(t.amount||0),0),incoming=successful.filter(t=>t.direction==='credit').reduce((s,t)=>s+Number(t.amount||0),0);const cats=successful.filter(t=>t.direction==='debit').reduce((a,t)=>{const k=t.type||'Other';a[k]=(a[k]||0)+Number(t.amount||0);return a},{});return <div><div className="page-heading"><small>INSIGHTS</small><h1>Analytics</h1><p>Understand your real activity without drowning in charts.</p></div><div className="metric-grid"><div><small>SPENDING</small><b>{money(out)}</b><span>This period</span></div><div><small>MONEY IN</small><b>{money(incoming)}</b><span>This period</span></div></div><section className="panel chart-panel"><div className="section-heading"><div><small>SPENDING OVERVIEW</small><h2>Where money is going</h2></div></div><div className="bar-chart">{Object.entries(cats).slice(0,7).map(([k,v])=><div key={k}><span style={{height:`${Math.max(10,Math.min(100,v/Math.max(out,1)*100))}%`}}/><b>{k}</b></div>)}</div></section></div>}
