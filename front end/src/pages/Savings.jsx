import {useState} from 'react';import {Plus,Target} from 'lucide-react';import {money} from '../utils/currency';export default function Savings(){const [goals,setGoals]=useState([{name:'Emergency Fund',target:500000,saved:120000},{name:'Laptop',target:350000,saved:0}]);return <div><div className="page-heading page-heading-row"><div><small>GOALS</small><h1>Savings</h1><p>Create a target and make progress visible.</p></div><button className="primary" onClick={()=>setGoals([...goals,{name:'New goal',target:100000,saved:0}])}><Plus/>New goal</button></div><div className="savings-grid">{goals.map(g=>{const pct=Math.min(100,Math.round(g.saved/g.target*100));return <article className="saving-card" key={g.name}><span className="goal-icon"><Target/></span><h2>{g.name}</h2><strong>{money(g.saved)} <small>/ {money(g.target)}</small></strong><div className="progress"><span style={{width:`${pct}%`}}/></div><div><b>{pct}%</b><button>Add money</button></div></article>})}</div></div>}
