import {Link} from 'react-router-dom';import {ArrowLeft,Mail} from 'lucide-react';export default function ForgotPassword(){return <div className="simple-auth"><div className="simple-auth-card"><span className="simple-icon"><Mail/></span><small>ACCOUNT RECOVERY</small><h1>Forgot your password?</h1><p>Password reset isn't available from the current backend yet. Once the recovery endpoint is added, this screen is ready to connect.</p><Link className="primary link-button" to="/login"><ArrowLeft/>Back to sign in</Link></div></div>}
